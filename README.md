# Manteigas Trilhos Verdes — filtros

Static HTML page generated from manteigastrilhosverdes.com trail data, GPX/KML files and estimated elevation gain/loss.

Favorites are stored locally in the browser via localStorage; no backend/database is used.
